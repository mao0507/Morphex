{
  "name": "morphex-desktop",
  "private": true,
  "version": "0.4.2",
  "scripts": {
    "prepare-sidecar": "node scripts/prepare-sidecar.mjs",
    "tauri": "tauri",
    "dev": "npm run prepare-sidecar && tauri dev",
    "build": "npm run prepare-sidecar && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "cross-env": "^7"
  }
}
